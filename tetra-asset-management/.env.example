# Backend Configuration
BACKEND_PORT=3001
DATABASE_URL="postgresql://admin:password@localhost:5432/tetra_dev?schema=public" # For local development outside Docker
JWT_SECRET="your-super-secret-jwt-key"
JWT_EXPIRES_IN="1d"

# Frontend Configuration
FRONTEND_PORT=3000
VITE_API_URL="http://localhost:3001" # URL for the backend API

# Docker Postgres Configuration (used by docker-compose.yml if not overridden)
DATABASE_USER=admin
DATABASE_PASSWORD=password
DATABASE_NAME=tetra_dev
